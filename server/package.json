{
  "name": "pc-login-control-server",
  "version": "1.0.0",
  "private": true,
  "description": "PC Login Control — Vercel サーバーレスAPI（Supabase バックエンド）",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.6.0"
  }
}
